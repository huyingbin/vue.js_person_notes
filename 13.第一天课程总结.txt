1. MVC和MVVM的区别
2. 学习了vue的基本代码结构
3. 插值表达式 v-clock v-text v-html v-bind(缩写:) v-on(缩写：@) v-model v-for v-if v-show
4. 事件修饰符：.stop .prevent .capture .self .once
5. el 指定要控制的区域  ， data是个对象，指定控制区域所用到的数据 ，methods:对象，用于用户自定义方法
6. 在VM实例中，如果要访问data上的数据，或者methods上的方法，必须带this
7. 在v-for中，要学会使用key（只接受number和string）
8. v-model只用于表单元素: input select textarea button
9. vue样式的绑定方式 :class  :style